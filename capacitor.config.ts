import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.robertmengual.toberquizz',
  appName: 'ToberQuizz',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
